interface:
  display_name: "Amazon 图片工作台"
  short_description: "策划 Amazon Listing 与 A+ 图片"
  default_prompt: "$amazon-image-studio：请按“一个窗口一个 SKU”的方式制作 Amazon Listing 或 A+ 图片；Listing 默认简约，A+ 大图模块默认内容丰富、按模块分工并避免重复构图，并在 Prompt 方案旁显示可选的风格模板图。"
